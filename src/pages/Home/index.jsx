import { Typography } from '@mui/material';
import React from 'react';

const Home = () => {
    return (
        <>
            <Typography variant="h1"> HOME </Typography>
        </>
    );
};

export default Home;
